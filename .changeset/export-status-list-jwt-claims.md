---
'@owf/token-status-list': patch
---

Export `verifyStatusListJwtClaims`, so the claims of a Status List Token in JWT format can be verified without also requiring the status at an index to be valid.
